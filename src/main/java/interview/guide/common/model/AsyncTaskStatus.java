package interview.guide.common.model;

/**
 * async task status
 * for async task for knowledge vectorize and resume analyze task
 */
public enum AsyncTaskStatus {
        PENDING,
        PROCESSING,
        COMPLETED,
        FAILED
}
